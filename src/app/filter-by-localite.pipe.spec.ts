import { FilterByLocalitePipe } from './filter-by-localite.pipe';

describe('FilterByLocalitePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByLocalitePipe();
    expect(pipe).toBeTruthy();
  });
});
